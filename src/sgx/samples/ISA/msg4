Server to Client:

1. Verify Ga in msg3 matches Ga in msg1.

2. Verify CMAC ......

3. Verify everything of msg3 sent by the Client.
- Submit the quote from msg3 to the IAS with API function for verification.
- After the API call, a report is received, and it should be validated.
- Validate the report signature using the public key in the email.

4. Validate the Enclave identity ......

5. Return encrypted secret to the Client, this secret should be encrypted using the key derived from the KDK.
Problem: we might need 

6. Inside the enclave, the secret can be decrypted with the function sgx_rijndael128GCM_decrypt using the key generated by sgx_ra_get_keys function.
